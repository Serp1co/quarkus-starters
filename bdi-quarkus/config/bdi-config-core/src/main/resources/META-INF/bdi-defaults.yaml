# Standardized defaults of bdi-config-core (ordinal 100: below application.yaml and the platform).
quarkus:
  banner:
    enabled: false
  config:
    build-time-mismatch-at-runtime: fail   # a build-time key rendered with another value stops the start instead of warning (design note 2.2)
