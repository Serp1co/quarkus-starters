{
  "module": "bdi-config-scheduler",
  "keys": [
    { "name": "quarkus.quartz.clustered", "type": "boolean", "default": "true", "phase": "build-time", "doc": "Quartz cluster mode: true whenever more than one instance runs (fixed at build time)" },
    { "name": "quarkus.scheduler.enabled", "type": "boolean", "default": "true", "phase": "build-time", "doc": "Scheduler on or off: fixed at build time (a per-instance switch is quarkus.quartz.start-mode, runtime)" }
  ]
}
