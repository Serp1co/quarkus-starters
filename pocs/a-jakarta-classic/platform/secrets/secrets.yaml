# ---------------------------------------------------------------------------
# Delivered at deploy time from the vault (CyberArk or HashiCorp, whichever the estate uses), never from
# inventory: a 0600 file or a systemd credential on a VM, a podman secret on stage 2, a Secret volume on
# OpenShift. Same path on every stage, named in QUARKUS_CONFIG_LOCATIONS next to the rendered configuration.
# This copy holds the credentials of the local sandbox database only.
# ---------------------------------------------------------------------------
quarkus:
  datasource:
    password: poc
